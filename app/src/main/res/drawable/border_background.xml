<!-- border_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Fond transparent -->
    <stroke android:width="2dp" android:color="@color/black" /> <!-- Bordure de 2dp -->
    <corners android:radius="8dp" /> <!-- Coins arrondis -->
    <padding
        android:left="8dp"
        android:right="8dp"
        android:top="4dp"
        android:bottom="4dp" />
</shape>